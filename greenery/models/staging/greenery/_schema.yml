
version: 2

models:
  - name: stg_greenery__users
    description: "Staging model for users"
    columns:
      - name: user_guid
        description: "Global unique ID for Greenery users"
        tests:
          - unique
          - not_null